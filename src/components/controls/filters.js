import { Button } from "@mui/material";
import React from "react";

const Filters = () => {
  return (
    <>
      <Button variant="contained">All</Button>
      <Button variant="contained">States</Button>
      <Button variant="contained">UTs</Button>
    </>
  );
};

export default Filters;
